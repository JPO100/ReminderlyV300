Claude,
Before doing anything, re-read /docs/Claude.md in full.
Do not rely on memory.
Reminderly is an app, not a rocket ship to Mars. Keep this implementation simple and lightweight.
Do not begin implementation until you confirm the scope exactly as written.

Lists feature flag rule
All changes in this instruction must be fully gated behind the existing state variable:
isListsEnabled
When isListsEnabled === false, the application must behave exactly as it does today.

Objective
Implement a bottom slide-up overlay that opens when the Add new list button is pressed on the Lists tab.
The overlay must follow the exact same structural pattern used by the existing Add new reminder overlay, including:
* slide-up animation
* positioning
* height
* responsive behaviour
* backdrop behaviour
* padding
* spacing
The reminder overlay must not be modified.
The Lists overlay must be implemented separately.

Files allowed to change
Only this file may be modified:
/src/app/App.tsx
No other files may be edited.
No new files may be created.

Existing state variables
The following already exist in /src/app/App.tsx:
isListsEnabled
activeMainTab
viewMode

Change 1 — Lists overlay state
Add a new state variable in /src/app/App.tsx:
isListsOverlayOpen
Type:
boolean
Initial value:
false

Change 2 — Add new list button behaviour
Locate the button with text:
Add new list
Modify the button so that clicking it sets:
isListsOverlayOpen = true
This behaviour must only run when:
isListsEnabled === true
AND
activeMainTab === 'lists'
AND
viewMode !== 'lists-done'

Change 3 — Overlay render branch
Render the Lists overlay when:
isListsEnabled === true
AND
activeMainTab === 'lists'
AND
isListsOverlayOpen === true
Place this render block in the same section of /src/app/App.tsx where the existing reminder overlay is rendered.
Do not modify the reminder overlay.

Change 4 — Overlay structure
Duplicate the structural pattern used by the existing Add new reminder overlay.
The Lists overlay must contain:
1. backdrop layer
2. sliding panel container
3. content area
The overlay must slide up from the bottom of the viewport.

Change 5 — Overlay content
Inside the overlay content area render exactly one line of text:
Tap here to name your list...
Do not render:
* input fields
* buttons
* icons
* subtitles
* additional UI elements

Change 6 — Overlay close behaviour
The Lists overlay must close when the user taps the backdrop.
When the backdrop is tapped set:
isListsOverlayOpen = false
Do not modify the reminder overlay close behaviour.

Render rules
When
isListsEnabled === false
the application behaves exactly as today.
When
isListsEnabled === true
AND
activeMainTab === 'lists'
AND
viewMode !== 'lists-done'
clicking Add new list opens the Lists overlay.
When
viewMode === 'lists-done'
the previously implemented "Lists done area" placeholder must continue to render exactly as before.

Explicit non-scope
Do not modify:
/src/app/config/features.ts
/src/app/utils/featureFlags.ts
/src/app/dev/dev-tools-checks.ts
Do not modify:
* reminder scheduling
* reminder parsing
* reminder storage
* reminder filtering
* reminder ordering
* reminder completion behaviour
* reminder overlay logic
* dev tools
* feature flags
* tests
* documentation
Do not introduce:
* list models
* list storage
* list creation logic
* validation logic
* new components
* new files

Verification
Confirm the following behaviour:
With
isListsEnabled = false
the application behaves exactly as today.
With
isListsEnabled = true
AND activeMainTab = 'lists'
AND viewMode !== 'lists-done'
clicking Add new list opens a bottom slide-up overlay.
The overlay displays exactly:
Tap here to name your list...
Tapping the backdrop closes the overlay.

Before proceeding confirm:
1. Only /src/app/App.tsx will be modified
2. Lists overlay state will be introduced
3. Reminder overlay behaviour will remain unchanged
4. All Lists overlay behaviour will be gated behind isListsEnabled