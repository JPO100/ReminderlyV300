Scope correction

Two issues must be corrected.

1. Section titles appear in the copied self-check report output but are not rendered in the on-screen self-check results list.

2. The total number of checks is inconsistent between the runtime output, the baseline file, and the implementation summary.

No other work is permitted.

---

Required fix 1 - On-screen section titles

Render the same section titles in the on-screen self-check results list that already appear in the copied text output.

Requirements

* Use the exact same section titles already present in the copied output.
* Preserve the existing check order exactly.
* Preserve every check entry exactly as it currently appears.
* Preserve existing pass/fail indicators exactly as they are.
* Do not change check names.
* Do not change grouping structure.
* Do not change totals.

Implementation constraint

Use the same grouping logic already implemented for the copied output.

Do not introduce a second grouping system.

Do not modify the copied output behaviour.

Do not introduce UI redesign, layout changes, controls, filters, search, or new styling systems.

Only render the missing section titles in the on-screen list.

---

Required fix 2 - Correct total check count

The total number of checks must be consistent everywhere.

Determine the real number of executed checks by counting the checks that run at runtime across all suites.

Then ensure the same number appears in all of the following:

* runtime self-check report
* `/src/app/dev/BASELINE.md`
* documentation that references the total check count
* the implementation summary

Do not guess the number.

Do not add or remove checks.

Only correct the reported totals so they match the real executed total.

---

Non-scope

The following changes are not permitted:

* modifying any existing self-check logic
* renaming checks
* reordering checks
* adding new tests
* removing tests
* modifying reminder behaviour
* modifying application behaviour
* adding new UI features
* redesigning the dev tools overlay
* introducing new rendering systems
* refactoring unrelated code
* modifying documentation beyond correcting the self-check totals

---

Acceptance criteria

The work is complete only when all of the following are true:

1. Section titles appear in the copied self-check report.
2. The same section titles appear in the on-screen self-check results list.
3. The section ordering is identical in both outputs.
4. The total number of checks is identical across:

   * runtime self-check report
   * `/src/app/dev/BASELINE.md`
   * documentation referencing the self-check count
   * the implementation summary.
5. No check logic, wording, ordering, or grouping has changed.

---

Return

1. Files changed
2. Cause of the on-screen section header issue
3. Exact fix applied
4. Verified total number of executed checks
5. Confirmation that runtime output, baseline, and documentation totals now match exactly
